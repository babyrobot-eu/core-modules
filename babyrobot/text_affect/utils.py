import babyrobot.text_affect.config as ta_config


def load_lexicon():
    pass
    #with open(ta_config.AFFECTIVE_LEXICON) as fd:
    #    lines = [l.strip().split(' ') for l in fd.readlines()]
    #lexicon = {l[0]: map(int, l[1:]) for l in lines}
    #return lexicon

